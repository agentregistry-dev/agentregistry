<!-- Code generated by OSS E2E tests; DO NOT EDIT. -->

# Kagent Agent E2E scenario

## Run locally

Start the registry port-forward in a separate terminal:

```shell
kubectl --context "kind-${KIND_CLUSTER_NAME:-agentregistry}" -n "${KIND_NAMESPACE:-agentregistry}" port-forward svc/agentregistry 18121:12121
```

Set the scenario variables:

```shell
export KIND_CLUSTER_NAME="${KIND_CLUSTER_NAME:-agentregistry}"
export KIND_NAMESPACE="${KIND_NAMESPACE:-agentregistry}"
export KAGENT_NAMESPACE="${KAGENT_NAMESPACE:-kagent}"
export E2E_ID="${E2E_ID:-$(date +%s)}"
export ARCTL_API_BASE_URL="${ARCTL_API_BASE_URL:-http://localhost:18121/v0}"
```

## Configure the Kagent runtime

Create a Secret and a Kagent Runtime that reads its bearer token from the Secret API.

```shell
arctl apply -f - <<EOF
apiVersion: ar.dev/v1alpha1
kind: Secret
metadata:
  name: e2e-${E2E_ID}-secret
spec:
  type: Opaque
  stringData:
    token: e2e-token
---
apiVersion: ar.dev/v1alpha1
kind: Runtime
metadata:
  name: e2e-${E2E_ID}-runtime
spec:
  type: Kagent
  config:
    kagentUrl: http://kagent-controller.${KAGENT_NAMESPACE}:8083
    namespace: ${KAGENT_NAMESPACE}
    auth:
      secretRef:
        name: e2e-${E2E_ID}-secret
        key: token
EOF
```

## Deploy an Agent

Create a Model and Agent, then deploy the Agent to Kagent using modelRef.

```shell
arctl apply -f - <<EOF
apiVersion: ar.dev/v1alpha1
kind: Model
metadata:
  name: e2e-${E2E_ID}-model
  tag: e2e
spec:
  title: Kagent E2E model
  provider: bedrock
  model: anthropic.claude-3-5-sonnet-20241022-v2:0
  auth:
    strategy: runtime
  endpoint:
    region: us-west-2
---
apiVersion: ar.dev/v1alpha1
kind: Agent
metadata:
  name: e2e-${E2E_ID}-agent
spec:
  source:
    image: registry.k8s.io/pause:3.10
    protocol: A2A
EOF
```

```shell
arctl apply -f - <<EOF
apiVersion: ar.dev/v1alpha1
kind: Deployment
metadata:
  name: e2e-${E2E_ID}-deployment
spec:
  targetRef:
    kind: Agent
    name: e2e-${E2E_ID}-agent
  runtimeRef:
    kind: Runtime
    name: e2e-${E2E_ID}-runtime
  modelRef:
    name: e2e-${E2E_ID}-model
    tag: e2e
EOF
```

## Reapply the Deployment

Reapply the unchanged Deployment and verify Kagent does not replace the Agent.

```shell
arctl apply -f - <<EOF
apiVersion: ar.dev/v1alpha1
kind: Deployment
metadata:
  name: e2e-${E2E_ID}-deployment
spec:
  targetRef:
    kind: Agent
    name: e2e-${E2E_ID}-agent
  runtimeRef:
    kind: Runtime
    name: e2e-${E2E_ID}-runtime
  modelRef:
    name: e2e-${E2E_ID}-model
    tag: e2e
EOF
```

## Remove the Agent Deployment

Delete the AgentRegistry Deployment and verify Kagent removes the Agent workload.

```shell
arctl delete deployment e2e-${E2E_ID}-deployment
```

```shell
kubectl --context kind-${KIND_CLUSTER_NAME} -n "${KAGENT_NAMESPACE}" wait --for=delete agent/e2e-${E2E_ID}-agent --timeout=2m
```

```shell
arctl delete agent e2e-${E2E_ID}-agent
```

```shell
arctl delete model e2e-${E2E_ID}-model --tag e2e
```

```shell
arctl delete -f - <<EOF
apiVersion: ar.dev/v1alpha1
kind: Secret
metadata:
  name: e2e-${E2E_ID}-secret
spec:
  type: Opaque
  stringData:
    token: e2e-token
---
apiVersion: ar.dev/v1alpha1
kind: Runtime
metadata:
  name: e2e-${E2E_ID}-runtime
spec:
  type: Kagent
  config:
    kagentUrl: http://kagent-controller.${KAGENT_NAMESPACE}:8083
    namespace: ${KAGENT_NAMESPACE}
    auth:
      secretRef:
        name: e2e-${E2E_ID}-secret
        key: token

EOF
```
