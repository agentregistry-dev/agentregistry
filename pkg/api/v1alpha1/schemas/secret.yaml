description: SecretSpec is the desired metadata and write-only payload.
properties:
  data:
    additionalProperties:
      type: string
    description: data is part of SecretSpec.
    type: object
  immutable:
    description: immutable is part of SecretSpec.
    type: boolean
  stringData:
    additionalProperties:
      type: string
    description: stringData is part of SecretSpec.
    type: object
  type:
    description: type is part of SecretSpec.
    type: string
type: object
